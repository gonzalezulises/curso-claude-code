# Reglas del proyecto

> Copia este archivo a la raíz de tu proyecto. Claude Code lo lee al empezar
> cada sesión, sin que se lo pidas. Borra lo que no aplique: un archivo largo
> que se cumple a medias es peor que uno corto que se cumple entero.

## Qué es este proyecto

<!-- Dos frases: qué hace y para quién. Sin esto, el agente lo infiere del
     código y acierta la mitad de las veces. -->

## Comandos

```bash
# instalar
# desarrollo
# pruebas
# compilar
```

<!-- Los comandos exactos. Es lo que más veces te va a ahorrar escribir y lo
     que evita que invente un `npm start` que no existe. -->

## Convenciones

<!-- Solo las que ya rompiste alguna vez. Cada una de estas debería poder
     rastrearse a una corrección real que tuviste que hacer. -->

## Qué no tocar

<!-- Migraciones aplicadas, archivos generados, configuración de despliegue,
     `.env`. Es la sección que más ahorra y la que menos se escribe. -->

## Cuándo algo está terminado

<!-- La condición de aceptación por defecto. Sin esto el agente decide él, y
     decide antes de tiempo. Ejemplo: compila, las pruebas pasan, y el cambio
     se verificó en el navegador. -->

## Si no estás seguro

<!-- Preguntar, o proponer y seguir. Elige uno. Si no lo dices, cada sesión
     hará una cosa distinta. -->
